<?xml version="1.0" encoding="utf-8"?>
<!--
    El layout no contiene texto ni controles secundarios: todo el widget es
    una única acción de voz y el proveedor conecta el clic al PendingIntent.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_voice_capture_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_voice_background"
    android:clickable="true"
    android:contentDescription="@string/widget_voice_capture_description"
    android:focusable="true"
    android:foregroundGravity="center"
    android:gravity="center"
    android:padding="8dp">

    <ImageView
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:contentDescription="@null"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_mic" />

</FrameLayout>
