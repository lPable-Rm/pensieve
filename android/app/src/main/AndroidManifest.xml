<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 13+: permite mostrar recordatorios tras el consentimiento del usuario. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Solo se solicitará al pulsar la acción de crear una nota por voz. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />

    <!-- La app sigue siendo útil con texto aunque el dispositivo no tenga micrófono. -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="false" />

    <!-- Da visibilidad al servicio de reconocimiento en Android 11 o superior. -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".PensieveApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Pensieve">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Pensieve"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Entrada privada del widget hacia la única Activity real de Pensieve. -->
        <activity-alias
            android:name=".WidgetVoiceEntry"
            android:exported="false"
            android:targetActivity=".MainActivity" />

        <service
            android:name=".voice.VoiceCaptureService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- El launcher necesita este receiver exportado para alojar el widget. -->
        <receiver
            android:name=".widget.VoiceCaptureWidgetProvider"
            android:exported="true"
            android:label="@string/widget_voice_capture_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/voice_capture_widget_info" />
        </receiver>

        <!-- Solo los PendingIntent creados por Pensieve pueden completar una nota. -->
        <receiver
            android:name=".notifications.CompleteReminderReceiver"
            android:exported="false" />
    </application>

</manifest>
